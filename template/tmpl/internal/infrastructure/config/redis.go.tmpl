{{/*
Copyright (c) 2025 dung13890. All rights reserved.
Mit License (MIT)
*/}}

{{define "internal/infrastructure/config/redis.go"}}package config

import (
	"sync"

	"{{.Project}}/pkg/logger"

	"github.com/spf13/viper"
)

var (
	onceRedis sync.Once
	redisConf Redis
)

// Redis config struct
type Redis struct {
	Host     string `mapstructure:"REDIS_HOST"`
	Port     int    `mapstructure:"REDIS_PORT"`
	Password string `mapstructure:"REDIS_PASSWORD"`
}

// GetRedisConfig Unmarshal Redis Config from env
func GetRedisConfig() Redis {
	onceRedis.Do(func() {
		if err := viper.Unmarshal(&redisConf); err != nil {
			logger.Error(err)
		}
	})

	return redisConf
}
{{end}}
